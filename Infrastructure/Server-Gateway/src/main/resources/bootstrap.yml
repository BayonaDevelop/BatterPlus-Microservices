server:
  port: 8090

spring:
  application:
    name: server-gateway
  profiles:
    active: dev
    cloud:
      config:
        uri: http://localhost:8888
        name: infrastructure
      discovery:
        enabled: true

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty.http.client: DEBUG
